require(['jquery', 'makeSticky'], function ($) {

    $(".sticky").makeSticky();

});


  /* If you don't use JAM then just include the js/makeSticky.js plugin at the bottom of the html page and use as a normal plugin */
